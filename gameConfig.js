/**
 * ⚙️ GAME BALANCE CONFIG
 * 
 * Все параметры боевого баланса в одном месте.
 * Можно менять прямо тут и перезапускать сервер,
 * или менять на лету через API:
 * 
 *   GET  http://localhost:3000/api/config          — посмотреть текущие значения
 *   POST http://localhost:3000/api/config           — обновить (JSON body с нужными полями)
 * 
 * Пример запроса для обновления (curl / Postman / fetch):
 *   POST /api/config
 *   { "BASE_ATTACK_INTERVAL_MS": 2000, "TAP_BOOST_PER_TAP": 0.08 }
 */

const gameConfig = {

  // ═══════════════════════════════════════════════
  // HP (здоровье)
  // ═══════════════════════════════════════════════

  /** Базовое HP при endurance = 1 */
  BASE_HP: 80,

  /** Сколько HP добавляется за каждый пункт endurance (1→10 = 9 шагов) */
  HP_PER_ENDURANCE: 70 / 9,  // ~7.78

  // ═══════════════════════════════════════════════
  // СКОРОСТЬ УДАРОВ
  // ═══════════════════════════════════════════════

  /** Интервал атаки (мс) при agility = 1 (самый медленный) */
  BASE_ATTACK_INTERVAL_MS: 3000,

  /** Минимальный интервал атаки (мс) при agility = 10 (самый быстрый) */
  MIN_ATTACK_INTERVAL_MS: 2900,

  // ═══════════════════════════════════════════════
  // БУСТ ОТ КЛИКОВ (TAP BOOST)
  // ═══════════════════════════════════════════════

  /** На сколько % уменьшается интервал за КАЖДЫЙ тап (0.05 = 5%) */
  TAP_BOOST_PER_TAP: 0.1,

  /** Максимальное суммарное уменьшение интервала от тапов (0.5 = 50%) */
  TAP_BOOST_MAX_REDUCTION: 0.9,

  // ═══════════════════════════════════════════════
  // УРОН
  // ═══════════════════════════════════════════════

  /** Множитель strength для базового урона */
  DAMAGE_STRENGTH_MULTIPLIER: 3,

  /** Бонус урона: случайное число от 0 до (agility * этот множитель) */
  DAMAGE_AGILITY_BONUS_MULTIPLIER: 1,

  // ═══════════════════════════════════════════════
  // ТИКИ БОЯ
  // ═══════════════════════════════════════════════

  /** Частота тиков боевого цикла (мс). Чем меньше — тем плавнее, но тяжелее. */
  FIGHT_TICK_MS: 100,

  // ═══════════════════════════════════════════════
  // КОЭФФИЦИЕНТЫ СТАВОК
  // ═══════════════════════════════════════════════

  /** Веса характеристик для расчёта шансов (odds) */
  ODDS_STRENGTH_WEIGHT: 0.4,
  ODDS_AGILITY_WEIGHT: 0.3,
  ODDS_ENDURANCE_WEIGHT: 0.3,
};

export default gameConfig;
